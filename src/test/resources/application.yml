spring:
    application:
        name: fc-board
    profiles:
        active: console-logging, test-level-logging
    datasource:
      url: jdbc:h2:mem:test
      username: sa
      password:
      driver-class-name: org.h2.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
          hibernate:
              format_sql: true
    cache:
        type: redis
        redis:
            host: 158.247.251.28
            port: 16379
